on:
  workflow_dispatch:

concurrency:
  group: foo
  cancelInProgress: 1

jobs:
  my_job:
    name: deploy to staging
    runs-on: ubuntu-22.04
    steps:
    - name: Sleep for 30 seconds
      run: sleep 30s
      shell: bash
