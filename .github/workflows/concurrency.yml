on:
  workflow_dispatch:

concurrency:
  group: foo
  cancel-in-progress: true

jobs:
  my_job:
    name: deploy to staging
    runs-on: ubuntu-22.04
    steps:
    - name: Sleep for 30 seconds
      run: sleep 30s
      shell: bash
